{
  "name": "backend-course-2025-7",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-all": "npm install",
    "start": "node main.js -- --host localhost --port 3000 --cache ./cache",
    "debug": "node --inspect main.js -- --host localhost --port 3000 --cache ./cache",
    "dev": "npx nodemon main.js -- --host localhost --port 3000 --cache ./cache"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.2",
    "pg": "^8.11.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^4.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
